<h1 class="mb-4 text-4xl md:mb-12 md:text-6xl leading-tight">Distance Calculator</h1>
<div class="mb-6 text-lg md:mb-12 md:text-xl">
  <p class="py-2">
    This form fetches a list of users who live within a specified distance from
    London City.
  </p>
  <p class="py-2" id="role-help">Please enter the radius in miles to begin the search</p>
</div>
<form
  id="distance-calculator"
  name="distance-calculator"
  action="/calculate"
  method="POST">
  <div class="flex flex-wrap" >
    <div class="mb-6 md:mb-12 w-full">
      <label
        class="block uppercase tracking-wide text-gray-700 text-xs font-bold
          mb-2"
        for="grid-password">
        <span class="text-sm md:text-lg">Distance</span>
        <input
          class="appearance-none block w-full bg-gray-100 text-gray-700 border
            border-gray-500 rounded py-3 px-4 my-3 text-lg leading-tight
            focus:outline-none focus:bg-white focus:border-gray-700 {errors.distance ? 'border-red-700' : ''}"
          id="distance"
          name="distance"
          type="text"
          value=""
          pattern="^[0-9]\d{0,9}(\.\d{1,3})?%?$" 
          aria-describedby="role-help"
          placeholder="Distance (in miles) e.g.: 15.50" 
          required/>
      </label>
    </div>
    <div class="w-full flex flex-col md:flex-row">
      <button
        class="flex-shrink-0 bg-black hover:bg-black border-black text-lg 
          hover:bg-gray-800 hover:border-gray-800 text-sm border-4 text-white
          py-1 px-4 rounded mb-6 md:mb-auto"
        aria-label="Calculate Distance"
        role="button"
        type="submit">
        Calculate Distance
      </button>
      <button
        class="flex-shrink-0 border-transparent border-4 text-black
        text-lg hover:text-gray-500 text-sm py-1 px-4 rounded"
        aria-label="Reset Form"
        role="button"
        type="reset">
        Clear Results
      </button>
    </div>
  </div>
</form>